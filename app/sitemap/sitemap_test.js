'use strict';

describe('myApp.sitemap module', function() {

  beforeEach(module('myApp.sitemap'));

  describe('sitemap controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SitemapCtrl = $controller('SitemapCtrl');
      expect(SitemapCtrl).toBeDefined();
    }));

  });
});